# OpenAI-compatible chat and embedding endpoint.
# Leave empty to use deterministic demo mode.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# Optional overrides
PHARMASSIST_CONFIG=config/config.yaml
